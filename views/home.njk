{% extends "layout.njk" %}

{% block head %}
    <title>FoodFy</title>
{% endblock %}

{% block content%}
    <main>
        <div class="container">
            <section class="banner">
                <h2>As melhores receitas</h2>
                <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro</p>
                <img src="/assets/chef.png" alt="Imagem de chefe de cozinha segurando um prato delicioso.">
            </section>
        </div>

        <div class="container">
            <section class="recips">
                <h2>Mais acessadas</h2>

                <div class="recipes-container">

                    {% for item in items %}
                        <div class="recipe" id="{{item.id}}">
                            <img src="{{item.image}}" alt="Lanche de bacon">
                            <h3>{{item.title}}</h3>
                            <p>{{item.author}}</p>
                        </div>
                    {% endfor %}

                </div>

            </section>
        </div>
    </main>

    <script src="/script.js"></script>

{% endblock %}